const assert = require("assert");

describe("Database2", function() {
  describe("constructor", function() {
    it("should create folders and setup basic environment", function() {
      assert.equal(true, true);
    });
  });
});
